apply plugin: 'java'
apply plugin: 'kotlin'

dependencies {
  compile "org.jetbrains.kotlin:kotlin-stdlib:${versions.kotlin}"
  compile "com.squareup.okio:okio:${versions.okio}"
  compile "com.fazecast:jSerialComm:${versions.jSerialComm}"
}
